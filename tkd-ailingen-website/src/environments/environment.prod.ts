export const environment = {
  production: true,
  instagram: {
    // Production Instagram access token
    // IMPORTANT: For production, consider storing this in a secure backend
    // and fetching it via API to avoid exposing the token in the client bundle
    accessToken: 'YOUR_INSTAGRAM_ACCESS_TOKEN'
  }
};
